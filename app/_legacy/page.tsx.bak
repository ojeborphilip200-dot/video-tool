import { redirect } from "next/navigation";

// The old editor at / has been retired. /editor is the tool now; anything
// landing on the bare route goes straight there. Old code is in app/_legacy/.
export default function Home() {
  redirect("/editor");
}
